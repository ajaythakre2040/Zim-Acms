## Packages
framer-motion | For beautiful page transitions and micro-interactions
date-fns | For formatting dates in article lists and dashboards
react-icons | Additional icons if Lucide is insufficient (though Lucide is primary)
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
Authentication is handled via Replit Auth (use-auth.ts).
Images will be handled via simple URL inputs for now (external hosting).
Content editing uses a styled textarea for Markdown/Text to ensure stability.
